export type ButtonColor = "gray" | "green" | "red" | "transparent";

export enum ButtonTypes {
  TEXT_AND_ICON = "TEXT_AND_ICON",
  ICON_ONLY = "ICON_ONLY",
  TEXT_ONLY = "TEXT_ONLY",
  EMPTY = "EMPTY",
}
